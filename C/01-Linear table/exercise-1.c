// 线性表-课后算法设计题

/*
    1. 设线性表存放在数组 A[arrsize]的前 elenum 个分量中，且递增有序。
    试写一算法，将 x 插入到线性表的适当位置上，以保持线性表的元素仍然是递增有序的，并分析算法的时间复杂度。
*/
void insert_sorted(int A[], int *elenum, int arrsize, int x)
{
    // 如果数组已满，无法插入新元素
    if (*elenum == arrsize)
    {
        printf("数组已满，无法插入新元素\n");
        return;
    }

    // 找到要插入的位置
    int i = 0;
    while (i < *elenum && A[i] < x)
    {
        i++;
    }

    // 将元素插入到相应位置
    for (int j = *elenum; j > i; j--)
    {
        A[j] = A[j - 1];
    }
    A[i] = x;

    // 更新元素个数
    (*elenum)++;
}
/**
 * 算法的时间复杂度分析：

   在最坏情况下，需要遍历整个数组找到插入位置，时间复杂度为 O(n)，其中 n 是数组中元素的个数。
   插入操作需要将元素后移，时间复杂度也为 O(n)。
   因此，整个插入算法的时间复杂度为 O(n)。
   需要注意的是，如果数组 A 使用链表等动态存储结构，插入操作可能会更加高效，因为无需移动元素，仅需要调整指针即可。
*
/

