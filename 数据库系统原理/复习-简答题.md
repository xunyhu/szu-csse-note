# 简答题复习-已考不背

## 第 1 章 数据库系统概论

1. 【2021.10】`数据库管理系统`的主要功能
2. 【2021.04】`数据库系统`的特点
3. 【2020.10】简述层次模型的数据结构和特点
4. 【2020.08】数据库系统三级模式结构的特点
5. 【2019.10】数据模型的分类
6. 【2019.04】简述外模式/模式映像及其如何保证数据的逻辑独立性。
7. 【2018.10】简述关系模型的优点

8. 数据模型的组成要素

   数据结构；数据操作；数据约束

9. 网状模型

   层次模型的扩充；允许结点有多余一个父结点；并可以有一个以上结点没有父结点。

10. 人工管理阶段的特点

    数据不保存; 应用程序管理数据; 数据面向应用

## 第 2 章 关系数据库

1. 【2023.04】`简述关系模型的三类完整性约束`
2. 【2022.10】简述关系模型完整性约束的检验方法。
3. 【2022.04】简述关系的两个不变性及其含义。
4. 【2021.10】简述关系的码和候选码的概念。
5. 【2021.04】简述关系数据库对关系限定的具体要求。
6. 【2019.10】`简述实体完整性约束`
7. 【2019.04】`简述参照完整性约束`
8. 【2018.10】`简述用户定义完整性约束。`

9. 关系数据库的术语

   表：也称为关系，一个二维的数据结构。  
   列：字段，属性。  
   行：元组，记录。  
   分量：元组中的一个属性值。  
   码：属性或属性组的值能用来唯一标识关系的元组，则称这些属性或属性组为该关系的码或键。  
   候选码：如果在关系中的一个码或键中，不能从中移去任何一个属性，否则它就不是这个关系的码或键，则称这样的码为候选码或候选键。  
   主码：从候选码中指定一个用来唯一标识关系的元组，成为主码或主键。  
   全码：所有属性集合才是主码  
   主属性：包含在任何一个候选码中的属性成为主属性或码属性。  
   非主属性：其他属性  
   外码：如果某个属性或属性组不是这个关系的主码或候选码，而是另一个关系的主码，称该属性为这个关系的外码或外键。  
   被参照关系：主关系，以外码作为主码的关系成为被参照关系。  
   参照关系称为从关系，外码所在的关系称为参照关系。  
   域：属性的取值范围  
   关系模式：在关系数据库中，关系模式是型，关系是值。

10. 数据库的数据完整性

    指的是正确性、相容性和一致性

11. 关系模式中可能存在的冗余和异常问题

    数据冗余、更新异常、插入异常、删除异常

12. 范式

    第一范式：设 R 为任一给定关系，如果 R 中每个列与行的交点处的取值都是不可再分的基本元素， 则 R 为第一范式。
    第二范式：设 R 为任一给定关系，若 R 为 1NF，且其所有非主属性都完全函数依赖于候选关键字，则 R 为第二范式。 （消除部分函数依
    赖关系）
    第三范式：设 R 为任一给定关系，若 R 为 2NF，且其每一个非主属性都不传递函数依赖于候选关键字，则 R 为第三范式。（消除了传递函
    数依赖关系）

## 第 3 章 数据库设计

1. 【2022.10】简述关系数据模型的优化方法。

2. 数据库的生命周期

   数据库分析与设计阶段，数据库的实现与操作阶段

3. 数据库设计的目标

   满足应用功能需求，良好的数据库性能

4. 数据库设计的内容

   数据库结构设计，数据库行为设计

5. 数据库设计的方法

   直观设计法、规范设计法（较为普通、常用）、计算机辅助设计法

6. 数据库设计的基本步骤

   需求分析、概念结构设计、逻辑结构设计、物理结构设计、数据库实施、数据库的运行和维护

## 第 4 章 SQL 与关系数据库基本操作

1. 【2023.04】简述`视图`的概念及其与三级模式中相对应的模式。
2. 【2022.10】简述 SQL 的核心组成部分
3. 【2022.04】简述`视图`与基本表的区别。
4. 【2021.04】`简述数据定义语言`的功能及其包括的主要 SQL 语句。
5. 【2020.10】简述使用`视图`的优点。
6. 【2020.08】`简述数据控制语言`的功能及主要 SQL 语句的作用。
7. 【2019.10】简述变量的含义及其属性。
8. 【2019.04】简述 SQL 调用及主要目的。
9. 【2018.10】`简述数据操纵语言`的功能及主要 SQL 语句。

10. SQL 的特点

    1）几乎所有重要的关系数据库管理系统都支持 SQL  
    2）简单易学  
    3）一种强有力的语言，可以进行复杂高级操作  
    4）不区分大小写

11. 索引

    概念：索引是 DBMS 根据表中的一列或若干列按照一定顺序建立的列值与记录行之间的对应关系表。实质上是一张描述索引列的`列值与原表中记录行之间一一对应关系的有序表`。

    作用：索引是提高数据文件访问效率的有效方法。

    弊端：索引是以`文件`的形式存储，DBMS 会将一个表的所有索引保存在同一个索引文件中，需要占用磁盘空间。  
     索引在`提高查询速度`的同时，却会`降低更新表的速度`。

12. HAVING 子句与 WHERE 子句差异

    1）WHERE 子句用于过滤数据行，HAVING 子句用于过滤分组。  
    2）HAVING 子句可包含聚合函数，WHERE 子句不行  
    3）WHERE 子句会在数据分组前过滤，HAVING 子句则会在数据分组后过滤。

## 第 5 章 数据库编程

1. 【2023.04】简述使用`存储过程`的优点
2. 【2022.10】简述删除存储函数的语句及注意事项
3. 【2022.04】简述存储过程与存储函数的区别
4. 【2021.10】简述`存储过程中常用的条件判断语句和循环语句`
5. 【2021.04】简述在 MySQL 中使用游标的原因。
6. 【2020.10】简述声明局部变量的注意事项。
7. 【2020.08】简述在 MySQL 中使用游标的具体步骤
8. 【2019.10】简述游标的含义及创建游标的语句
9. 【2019.10】简述变量的含义及其属性
10. 【2019.04】简述在`存储过程中局部变量与用户变量的区别`。
11. 【2018.10】简述使用游标的注意事项。

12. 局部变量 p127
    用来存储存储过程体中的临时结果，使用 DECLARE 语句来声明局部变量

## 第 6 章 数据库安全与保护

1. 【2023.04】简述`并发操作`带来数据不一致的主要原因及解决方法
2. 【2023.04】简述 MySQL 中备份数据的语句及其功能
3. 【2022.10】简述三种典型的`并发操作`问题
4. 【2022.04】简述 MySQL 中恢复数据的语句及数据恢复完毕之后对表解锁的语句
5. 【2021.04】简述完整性约束条件的作用对象及其含义。
6. 【2021.10】简述约束命名后，使用 ALTER TABLE 语句和 DROP TABLE 语句应该注意的问题
7. 【2021.10】简述数据库备份和数据恢复的含义
8. 【2020.10】简述`封锁`的工作原理。
9. 【2020.08】简述`封锁`的概念及`封锁`的类型。
10. 【2019.10】简述触发器的概念及其作用
11. 【2019.04】简述`封锁`的级别的内容
12. 【2018.10】简述事务的概念及事务的特征

13. 用户定义的完整性 p139

    非空约束、CHECK 约束、触发器

14. 触发器的类型 p142

    INSERT 触发器、UPDATE 触发器、DELETE 触发器
